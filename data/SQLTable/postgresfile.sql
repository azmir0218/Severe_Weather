drop table if exists storm;
create table storm (
	Latitude varchar,
	Longitude varchar,
	State varchar,
	Name varchar,
	Disaster varchar,
	Year_start varchar,
	Year_end varchar,
	Total_dam varchar,
	Deaths varchar
	
);

select * from storm